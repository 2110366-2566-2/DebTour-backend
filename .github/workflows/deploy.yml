on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Stop existing containters
        run: docker-compose down

      - name: Download Go dependencies
        run: go mod download

      - name: Build Go
        run: go build -o main .

      - name: Init swagger
        run: swag init

      - name: Build docker compose
        run: docker-compose up -d
  deploy:
    runs-on: self-hosted
    needs: build
    steps:
      - name: Run Go
        run: ./main
